#include <stdio.h>

int main(void) {
	int num;
	scanf("%d", &num);  // 구구단할 숫자 입력
	int i;
	int result;

	for (i = 1;i < 10;i++) { // 구구단 실행
		result = num * i;
		printf("%d * %d = %d\n", num, i, result);
	}
	return 0;
}
