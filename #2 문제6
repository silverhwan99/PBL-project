#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int N, X;					//변수 할당
	scanf_s("%d %d", &N, &X);	//변수 입력
	
	for (int i = 0; i < N; i++) {	//N회 만큼 시행
		int rum = rand() % 10;		//랜덤으로 수 출력

		if (rum < X) {			//X보다 작은 수 출력
			printf("%d ", rum);	
		}
	}
	return 0;
}
